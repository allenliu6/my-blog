export const updataSidebar = ({commit}, bool) => {
    commit('UPDATA_SIDEBAR', bool)
}

export const updataHeader = ({commit}, bool) => {
    commit('UPDATA_HEADER', bool)
}

export const updataMobile = ({commit}, bool) => {
    commit('UPDATA_MOBILE', bool)
}